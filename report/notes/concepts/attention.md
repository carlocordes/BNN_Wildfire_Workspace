# Attention

A concept used in [[transformer]] which updates the embeddings of tokens, by evaluating their relative meanings.

- E.g. machine learning model -> fashion model
    - After attention, the term *model* has two very different vectors