# Padding
Used in the convolutional layer. By Adding a frame of zero-values around an input image, this assures that the same dimensions are carried over into feature space after a [[kernel]] is applied.

With padding, the kernel center can center every pixel individually and thus create an output of equal dimensionality.