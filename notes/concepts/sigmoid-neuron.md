# Sigmoid Neuron

A special type of [[perceptron]] as used in a[[neural-network]], described by a smooth [[activation-function]].

Produces continuous outputs based on a weight $w$ and bias $b$:
$$\sigma (wx + b) = \sigma (z) = \dfrac{1}{1 + e^{-z}}$$