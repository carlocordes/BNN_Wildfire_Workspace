# Perceptron

Model of an artificial neuron. It takes several binary inputs and outputs one binary output based on a weighted sum. Perceptron "fires" if this weighted sum exceeds a set threshold.

When combined they form [[multi-layer-perceptron]]

Further extensible as [[sigmoid-neuron]], which models a smooth [[activation-function]] rather than binary outputs.